# Convert JSONB to an R object

Convert JSONB to an R object

## Usage

``` r
from_jsonb(x, ...)
```

## Arguments

- x:

  a `blob` object to parse

- ...:

  arguments passed to yyjsonr::read_json_raw()
